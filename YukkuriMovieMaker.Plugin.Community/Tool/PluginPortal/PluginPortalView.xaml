<UserControl x:Class="YukkuriMovieMaker.Plugin.Community.Tool.PluginPortal.PluginPortalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YukkuriMovieMaker.Plugin.Community.Tool.PluginPortal"
             xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             xmlns:s="clr-namespace:YukkuriMovieMaker.Settings;assembly=YukkuriMovieMaker.Plugin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type={x:Type local:PluginPortalViewModel}}"
             >
    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <TextBox x:Name="SearchBox" VerticalContentAlignment="Center"
                             Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent" />

                    <TextBlock Text="{x:Static local:Texts.Search}" Foreground="Gray" VerticalAlignment="Center" Margin="5,0,0,0" IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>

                                    <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    
                </Grid>

                <Button x:Name="FilterButton" Grid.Column="1" Content="≡" Width="20" Click="FilterButton_Click">
                    <Button.ContextMenu>
                        <ContextMenu ItemsSource="{Binding TypeFilters}">
                            <ContextMenu.ItemContainerStyle>
                                <Style TargetType="MenuItem">
                                    <Setter Property="IsCheckable" Value="True"/>
                                    <Setter Property="Header" Value="{Binding Name}"/>
                                    <Setter Property="IsChecked" Value="{Binding IsChecked, Mode=TwoWay}"/>
                                    <Setter Property="StaysOpenOnClick" Value="True"/>
                                </Style>
                            </ContextMenu.ItemContainerStyle>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
            </Grid>

            <ListBox Grid.Row="1" BorderThickness="0,0,1,0"
                     ItemsSource="{Binding Plugins}" 
                     SelectedItem="{Binding SelectedPlugin, Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel LastChildFill="True">
                            <TextBlock Text="{Binding Name}"/>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="2" Content="{x:Static local:Texts.InstallDownloaded}" Command="{Binding InstallLocalCommand}" Height="25"/>
            
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="10">
                <StackPanel>
                    <TextBlock Text="{Binding SelectedPlugin.Name}" FontWeight="Bold" FontSize="22" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static local:Texts.Author}" FontWeight="SemiBold" Margin="0,0,10,8" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedPlugin.Author}" Margin="0,0,0,8"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static local:Texts.Type}" FontWeight="SemiBold" Margin="0,0,10,8" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedPlugin.Type}" Margin="0,0,0,8"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="{x:Static local:Texts.Date}" FontWeight="SemiBold" Margin="0,0,10,8" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedPlugin.Date, StringFormat='yyyy/MM/dd'}" Margin="0,0,0,8"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static local:Texts.URL}" FontWeight="SemiBold" Margin="0,0,10,8" HorizontalAlignment="Right"/>
                        <TextBlock Grid.Row="3" Grid.Column="1">
                        <Hyperlink Command="{Binding Source={x:Static s:CommandSettings.Default},Path=[OpenUri]}" CommandParameter="{Binding SelectedPlugin.Url}">
                            <TextBlock Text="{Binding SelectedPlugin.Url}" />
                        </Hyperlink>
                    </TextBlock>
                    </Grid>

                    <TextBlock Text="{x:Static local:Texts.Description}" FontWeight="Bold" Margin="0,20,0,5"/>
                    <Separator/>
                    <TextBlock Text="{Binding SelectedPlugin.Description}" Margin="0,5,0,0"/>

                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="1">
                <TextBlock Text="{Binding StatusMessage}" Margin="10,0"/>
                <Button Content="{x:Static local:Texts.DownloadFromGitHub}" Command="{Binding DownloadCommand}" Height="25" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
