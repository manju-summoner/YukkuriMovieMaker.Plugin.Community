<UserControl x:Class="YukkuriMovieMaker.Plugin.Community.Tool.PluginPortal.PluginPortalSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YukkuriMovieMaker.Plugin.Community.Tool.PluginPortal"
             xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Focusable="True">
        <Expander Header="{x:Static local:Texts.PluginPortal}" IsExpanded="True">
            <StackPanel>
                <Grid>
                    <Label Content="{x:Static local:Texts.IsCleanYmmeFile}"/>
                    <c:ToggleSlider Value="{Binding Source={x:Static local:PluginPortalSettings.Default},Path=IsCleanYmmeFile}" HorizontalAlignment="Right"/>
                </Grid>
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Source={x:Static local:PluginPortalSettings.Default}, Path=IsCleanYmmeFile}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <Label Content="{x:Static local:Texts.YmmeFilePath}"/>
                    <c:DirectorySelector Value="{Binding Source={x:Static local:PluginPortalSettings.Default}, Path=YmmeFilePath}" HorizontalAlignment="Right" Width="200"/>
                </Grid>
            </StackPanel>
        </Expander>
    </StackPanel>
</UserControl>
