<Window x:Class="YukkuriMovieMaker.Plugin.Community.Tool.Recording.RecordingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:YukkuriMovieMaker.Plugin.Community.Tool.Recording"
        Title="{x:Static local:Texts.RecordingWindowTitle}"
        Width="760"
        Height="520"
        MinWidth="680"
        MinHeight="460"
        xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
        xmlns:icons="clr-namespace:YukkuriMovieMaker.Resources.Icons;assembly=YukkuriMovieMaker.Plugin"
        >
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Static local:Texts.RecordingWindowTitle}"
                   FontWeight="Bold"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding DisplayText}"
                   FontSize="22"
                   FontWeight="SemiBold"
                   LineHeight="34"
                   TextWrapping="Wrap"/>

        <Separator Grid.Row="2" />

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static local:Texts.SaveDestination}" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" VerticalContentAlignment="Center" Height="26"/>
            <Button Grid.Column="2" Content="{DynamicResource {x:Static icons:IconKeys.FolderOpen}}" ToolTip="{x:Static local:Texts.Browse}" Command="{Binding BrowseOutputDirectoryCommand}"/>
            <Button Grid.Column="3" Content="{DynamicResource {x:Static icons:IconKeys.Refresh}}" ToolTip="{x:Static local:Texts.ResetToDefault}" Command="{Binding ResetOutputDirectoryCommand}"/>
        </Grid>

        <TextBlock Grid.Row="4"
                   Text="{x:Static local:Texts.OperationGuide}"
                   FontSize="12"
                   TextWrapping="Wrap"/>

        <ScrollViewer Grid.Row="5" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <WrapPanel>
                    <Button Content="{x:Static local:Texts.StartRecording}" Command="{Binding StartRecordingCommand}"/>
                    <Button Content="{x:Static local:Texts.StopRecording}" Command="{Binding StopRecordingCommand}"/>
                    <Button Content="{x:Static local:Texts.AddToTimeline}" Command="{Binding AddToTimelineCommand}"/>
                    <Button Content="{x:Static local:Texts.ReRecord}" Command="{Binding RegenerateCommand}"/>
                    <Button Content="{x:Static local:Texts.Play}" Command="{Binding PlayCommand}"/>
                </WrapPanel>

                <TextBlock Text="{x:Static local:Texts.InputLevel}"/>
                <ProgressBar Minimum="0" Maximum="1" Value="{Binding CurrentVolume}" Height="26"/>
            </StackPanel>
        </ScrollViewer>

        <TextBlock Grid.Row="6"
                   Text="{Binding Status}"
                   Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                   FontWeight="Bold"
                   TextWrapping="Wrap"/>
    </Grid>
</Window>
