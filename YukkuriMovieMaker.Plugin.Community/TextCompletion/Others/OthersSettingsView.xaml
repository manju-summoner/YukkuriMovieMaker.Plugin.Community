<UserControl x:Class="YukkuriMovieMaker.Plugin.Community.TextCompletion.Others.OthersSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YukkuriMovieMaker.Plugin.Community.TextCompletion.Others"
             xmlns:controls="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             xmlns:tc="clr-namespace:YukkuriMovieMaker.Plugin.TextCompletion;assembly=YukkuriMovieMaker.Plugin"
             xmlns:llm="clr-namespace:LlmTornado.Code;assembly=LlmTornado"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Expander Header="{x:Static local:Texts.Others}" IsExpanded="True">
            <StackPanel>
                <Grid>
                    <Label Content="{x:Static local:Texts.ServiceProvider}"/>
                    <controls:EnumComboBox Value="{Binding Source={x:Static local:OthersSettings.Default},Path=Provider}" EnumType="{x:Type llm:LLmProviders}" Width="200" HorizontalAlignment="Right"/>
                </Grid>
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Source={x:Static local:OthersSettings.Default},Path=Provider}" Value="Custom">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Source={x:Static local:OthersSettings.Default},Path=Provider}" Value="Unknown">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Source={x:Static local:OthersSettings.Default},Path=Provider}" Value="Length">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Label Content="{x:Static local:Texts.ServerUri}"/>
                    <TextBox Text="{Binding Source={x:Static local:OthersSettings.Default},Path=ServerUri}" Width="200" VerticalContentAlignment="Center" HorizontalAlignment="Right"/>
                </Grid>
                <tc:LlmTornadoTextCompletionGeneralSettingsView Settings="{Binding Source={x:Static local:OthersSettings.Default},Path=GeneralSettings}" Provider="{Binding Source={x:Static local:OthersSettings.Default},Path=Provider}"/>
            </StackPanel>
            
        </Expander>
    </StackPanel>
</UserControl>
