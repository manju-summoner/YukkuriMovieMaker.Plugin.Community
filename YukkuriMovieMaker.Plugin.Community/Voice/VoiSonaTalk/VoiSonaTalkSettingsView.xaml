<UserControl x:Class="YukkuriMovieMaker.Plugin.Community.Voice.VoiSonaTalk.VoiSonaTalkSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YukkuriMovieMaker.Plugin.Community.Voice.VoiSonaTalk"
             xmlns:ps="clr-namespace:YukkuriMovieMaker.Settings;assembly=YukkuriMovieMaker.Plugin"
             xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:VoiSonaTalkSettingsViewModel/>
    </UserControl.DataContext>
    <Expander Header="VoiSonaTalk" IsExpanded="True">
        <Expander.Resources>
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="Width" Value="200"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </Expander.Resources>
        <StackPanel>
            <Grid>
                <Label Content="VoiSona Talk.exe"/>
                <c:FileSelector Filter="VoiSona Talk|VoiSona Talk.exe;" 
                                FileType="None"
                                Value="{Binding Source={x:Static local:VoiSonaTalkSettings.Default}, Path=AppPath, Mode=TwoWay}"
                                Width="200"
                                HorizontalAlignment="Right"
                                ShowThumbnail="False"/>
            </Grid>
            <Grid>
                <Label Content="{x:Static local:Texts.UserName}"/>
                <TextBox Text="{Binding Source={x:Static local:VoiSonaTalkSettings.Default}, Path=UserName, Mode=TwoWay, Delay=1, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
            <Grid>
                <Label Content="{x:Static local:Texts.Password}"/>
                <TextBox Text="{Binding Source={x:Static local:VoiSonaTalkSettings.Default}, Path=Password, Mode=TwoWay, Delay=1, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
            <Grid>
                <Label Content="{x:Static local:Texts.Port}"/>
                <TextBox Text="{Binding Source={x:Static local:VoiSonaTalkSettings.Default}, Path=Port, Mode=TwoWay, Delay=1, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
            <Grid>
                <Label Content="{x:Static local:Texts.CharacterCount}"/>
                <Grid HorizontalAlignment="Right" Width="200">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="26"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding CharacterCountText}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,6,0"/>
                    <Button Grid.Column="1" Content="{DynamicResource ReloadMonochrome}" Command="{Binding RefreshCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Content" Value="{DynamicResource ReloadMonochrome}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                        <Setter Property="Content" Value="{DynamicResource loading}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
            <GroupBox Header="{x:Static local:Texts.HowToSetUp}" Margin="12,6,12,6" Padding="12,6,12,6" >
                <TextBlock TextWrapping="Wrap">
                    <Run Text="{x:Static local:Texts.HowToSetUpText}"/>
                    <LineBreak/>
                    <LineBreak/>
                    <Hyperlink Command="{Binding Source={x:Static ps:CommandSettings.Default},Path=[OpenUri]}" CommandParameter="https://manjubox.net/ymm4/faq/%E3%82%86%E3%81%A3%E3%81%8F%E3%82%8A%E3%83%9C%E3%82%A4%E3%82%B9/VoiSonaTalk/">
                        <Run Text="{x:Static local:Texts.HowToSetUpLink}"/>
                    </Hyperlink>
                    <LineBreak/>
                    <LineBreak/>
                    <Run Text="Copyright (c) 2023 Techno-Speech, Inc."/>
                </TextBlock>
            </GroupBox>
        </StackPanel>
    </Expander>
</UserControl>
