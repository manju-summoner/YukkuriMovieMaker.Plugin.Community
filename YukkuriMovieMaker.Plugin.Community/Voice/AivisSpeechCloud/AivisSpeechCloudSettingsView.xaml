<UserControl x:Class="YukkuriMovieMaker.Plugin.Community.Voice.AivisSpeechCloud.AivisSpeechCloudSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YukkuriMovieMaker.Plugin.Community.Voice.AivisSpeechCloud"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Expander Header="AivisSpeech Cloud" IsExpanded="True" Grid.Row="0">
            <Grid>
                <Label Content="{x:Static local:Texts.ApiKey}"/>
                <TextBox Text="{Binding Source={x:Static local:AivisSpeechCloudSettings.Default},Path=ApiKey,UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Right" VerticalContentAlignment="Center"/>
            </Grid>
        </Expander>
        <Expander Header="{x:Static local:Texts.SpeakerList}" IsExpanded="True" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="26"/>
                    <RowDefinition/>
                    <RowDefinition Height="26"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="{x:Static local:Texts.ModelUuid}"/>
                    <TextBox Grid.Column="1" x:Name="AddModelUuidTextBox" VerticalContentAlignment="Center"/>
                    <Button Grid.Column="2" x:Name="AddModelUuidButton" Content="{x:Static local:Texts.Add}" Click="AddModelUuidButton_Click"/>
                </Grid>
                <DataGrid Grid.Row="1" x:Name="ModelUuidDataGrid" ItemsSource="{Binding Source={x:Static local:AivisSpeechCloudSettings.Default},Path=Models}" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{x:Static local:Texts.NameHeader}" Binding="{Binding Name}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="{x:Static local:Texts.Description}" Binding="{Binding Description}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="{x:Static local:Texts.ModelUuid}" Binding="{Binding AivmModelUuid}" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="1" Content="{x:Static local:Texts.Delete}" Click="Button_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=ModelUuidDataGrid,Path=SelectedItem}" Value="{x:Null}">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
        </Expander>
    </Grid>
</UserControl>
