<UserControl x:Class="YukkuriMovieMaker.Plugin.Community.Voice.LivetoonTTS.LivetoonTTSSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YukkuriMovieMaker.Plugin.Community.Voice.LivetoonTTS"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Expander Header="Livetoon TTS" IsExpanded="True">
        <StackPanel>
            <Grid>
                <Label Content="{x:Static local:Texts.APIKey}" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Source={x:Static local:LivetoonTTSSettings.Default}, Path=APIKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" HorizontalAlignment="Right" Width="200"/>
            </Grid>
            <GroupBox Header="{x:Static local:Texts.About}" Margin="12">
                <TextBlock Text="{x:Static local:Texts.AboutMessage}" Margin="12"/>
            </GroupBox>
        </StackPanel>
    </Expander>
</UserControl>
